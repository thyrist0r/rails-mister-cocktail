<div class="container">
  <%= simple_form_for [@cocktail, @dose] do |f| %>
    <%= f.input :description %>
    <%= f.association :ingredient %>
    <%= f.submit "Add this dose of ingredient", class: "btn btn-primary" %>
  <% end %>
</div>
